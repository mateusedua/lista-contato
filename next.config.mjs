/** @type {import('next').NextConfig} */

const nextConfig = {
    env: {
        NEXT_PUBLIC_NEXT_PUBLIC_API_URL: 'https://api-lista-contato.mateuseduardoteuta10.workers.dev/'
    }
};

export default nextConfig;